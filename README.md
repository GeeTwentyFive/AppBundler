Program which takes in a .zip file, a path within the .zip file to an executable, and command line arguments for that executable, and creates a program which contains that .zip file within which, when run, extracts that .zip file in a temporary folder and runs the given program within with the given command line arguments 

# Usage
`AppBundler <TARGET>.zip <PATH/TO/APP/WITHIN> [ARGS...]`